apiVersion: v1
kind: Service
metadata:
  name: {{ include "vinaros.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vinaros.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vinaros.selectorLabels" . | nindent 4 }}
